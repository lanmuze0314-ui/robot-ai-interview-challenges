# PLAN

## 目标

实现一个简化的机器人迎宾应用，满足题目中的事件规则、状态去重、离场定时送客、snapshot 隔离，以及对应的测试与文档要求。

## 任务拆分

### 1. 需求拆解
- 梳理事件类型：`PERSON_ENTERED`、`PERSON_LEFT`、`CONVERSATION_STARTED`、`CONVERSATION_ENDED`、`MEETING_STARTED`、`MEETING_ENDED`、`TICK`
- 明确业务规则：
  - 空闲时首次进入要迎宾
  - 同一持续在场期间不重复迎宾
  - 对话/会议期间抑制迎宾和送客
  - 离场满 10 秒后由 `TICK` 触发送客
  - 短暂离开又返回不算真正离场
  - 完成离场确认后，再次进入视为新接待过程

### 2. 数据模型设计
- 在 `robot_application/models.py` 中定义：
  - `Event`
  - `Effect`
- 保持数据类轻量、冻结、便于测试和拷贝

### 3. 核心应用实现
- 在 `robot_application/application.py` 中实现 `RobotApplication`
- 维护内部状态：
  - 当前是否有人在场
  - 是否已经迎宾
  - 是否处于对话中
  - 是否处于会议中
  - 最近离场时间
  - 是否已经完成送客
- `handle_event()` 只返回本次事件新产生的效果
- `snapshot()` 返回外部不可反向修改内部状态的副本

### 4. 规则与状态机
- 先实现基础状态迁移
- 再补充抑制逻辑
- 再补充 `TICK` 的离场超时判断
- 再补充短暂离开返回、重复进入、再次接待等边界条件

### 5. 测试
- 使用 `pytest`
- 覆盖以下场景：
  - 首次迎宾与重复进入
  - 对话和会议抑制
  - 离场 10 秒后只送客一次
  - 短暂离开后返回
  - 完整离场后再次进入
  - `snapshot()` 隔离性

### 6. 文档
- `ARCHITECTURE.md`
  - 模块图
  - 模块职责边界
  - 状态归属
  - 扩展 VIP、RAG、ROS 2 的思路
- `REPORT.md`
  - 完成情况
  - 测试命令与结果
  - ROS 2 判断题答案
  - 未完成项或已知限制
- `AI_USAGE.md`
  - 使用了什么 AI
  - AI 帮了什么
  - 如何人工验证结果

## 实施顺序

1. 先完成数据模型
2. 再完成 `RobotApplication`
3. 再补测试
4. 再补架构文档和报告
5. 最后整体检查、运行测试、整理提交

## 验收标准

- 所有题目规则都能被代码正确处理
- 测试覆盖题目要求的主要场景
- `snapshot()` 不会泄露内部状态
- 代码结构清晰，没有全局可变状态
- 文档说明清楚，便于评审理解

## 风险与应对

- 风险：状态过多导致逻辑混乱
  - 应对：把“在场、对话、会议、离场计时、送客去重”拆成明确字段
- 风险：`TICK` 时序判断出错
  - 应对：专门写时间边界测试
- 风险：`snapshot()` 被外部修改
  - 应对：返回深拷贝或不可变结构，并写隔离测试
